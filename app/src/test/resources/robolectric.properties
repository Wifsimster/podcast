# Robolectric runs host-JVM tests against this Android API level. Pinned to 34
# (the highest level Robolectric 4.14 ships a runtime for) even though the app
# compiles against SDK 36 — the runtime SDK only affects the emulated framework.
sdk=34
